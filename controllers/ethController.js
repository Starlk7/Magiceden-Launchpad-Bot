const { sleep, formatTime, colors, settings } = require('../helper.js');
const { createDriver } = require('../webdriver.js');

async function ethController(){

}

async function ethImportWallet(driver, seedPhrase){

}

async function ethConnectToPage(driver, link, tNum, sNum){

}

async function ethHandleMint(driver, tNum, sNum){

}



module.exports = {
    ethController,
};